name: Build and test pull request

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: build pull request
    permissions:
      packages: read
    uses: ./.github/workflows/build.yml
